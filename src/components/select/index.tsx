import React from "react";
import "./style.css"

export const Select = () => {
  return (
    <select className="select">
      <option value={0}>TECH CAMP</option>
      <option value={1}>sample string</option>
    </select>
  );
};